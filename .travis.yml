sudo: required

language: node_js
node_js:
  - "5.0.0"

services:
  - docker

install:
  - npm install
  - docker-compose build

script:
  - npm run test:unit
  - npm run lint
  - docker-compose up -d
  - npm run test:integration
